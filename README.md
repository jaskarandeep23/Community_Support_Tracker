# Community Support Tracker
A web application to track community donations with validation, persistence, and CI testing.

## 🚀 Features
- **Stage 1:** Donation form with validation (charity, amount, date, comment).
- **Stage 2:** Donation table UI with localStorage persistence and total summary.
- **Stage 3:** GitHub Actions CI workflow running Jest + JSDOM tests, publishing results to PRs.

## 📦 Installation
Clone the repository and install dependencies:
```bash
git clone https://github.com/jaskarandeep23/Community_Support_Tracker.git
cd Community_Support_Tracker
npm ci
